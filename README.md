# ZORD MVP

ZORD MVP 1 is intentionally simple: a curated prompt/image gallery, prompt reuse, reference-image generation, user authentication/profiles, and lightweight publishing.

This repository is the implementation base for the first ZORD MVP. The current catalog uses deterministic placeholder images so the product can be built and tested immediately; replace them with original/licensed ZORD artwork before public launch.
