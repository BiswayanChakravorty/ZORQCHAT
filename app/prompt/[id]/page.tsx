import Link from "next/link"; import Header from "@/components/Header"; import CopyButton from "@/components/CopyButton"; import {catalog} from "@/lib/catalog"; import {notFound} from "next/navigation";
export default async function PromptPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const item=catalog.find(x=>x.id===id);if(!item)return notFound();return <><Header/><main className="container detail"><div><img className="detailImage" src={item.imageUrl} alt={item.title}/></div><div className="panel"><div className="eyebrow">{item.category}</div><h1 style={{fontFamily:"Space Grotesk",fontSize:42,letterSpacing:-2,margin:"10px 0"}}>{item.title}</h1><div className="meta" style={{marginBottom:25}}><span>ZORD ✓</span><span>{item.model} · {item.aspectRatio}</span></div><div className="label">Structured prompt</div><div className="promptBox">{item.prompt}</div><div style={{display:"flex",gap:10,flexWrap:"wrap"}}><CopyButton text={item.prompt}/><Link className="btn primary" href={`/create?prompt=${item.id}`}>Use This Prompt</Link></div><div style={{marginTop:28}} className="muted">Part of the ZORD Founding 100 collection.</div></div></main></>}
