import {NextResponse} from "next/server";import OpenAI from "openai";
export const runtime="nodejs";
function demoSvg(prompt:string){const safe=prompt.replace(/[<>&"]/g,"").slice(0,90);const svg=`<svg xmlns="http://www.w3.org/2000/svg" width="1024" height="1280"><defs><linearGradient id="g" x1="0" y1="0" x2="1" y2="1"><stop stop-color="#151515"/><stop offset="1" stop-color="#5a5a5a"/></linearGradient></defs><rect width="100%" height="100%" fill="url(#g)"/><circle cx="720" cy="300" r="180" fill="#ffffff" opacity=".12"/><text x="70" y="1050" fill="white" font-size="34" font-family="Arial">ZORD DEMO</text><text x="70" y="1100" fill="#ddd" font-size="20" font-family="Arial">${safe}</text></svg>`;return `data:image/svg+xml;base64,${Buffer.from(svg).toString("base64")}`}
export async function POST(req:Request){try{const {prompt,reference}=await req.json();if(!prompt)return NextResponse.json({error:"Prompt is required."},{status:400});if(!process.env.OPENAI_API_KEY)return NextResponse.json({image:demoSvg(prompt),demo:true});const openai=new OpenAI({apiKey:process.env.OPENAI_API_KEY});let response;if(reference){const base64=reference.includes(",")?reference.split(",")[1]:reference;const buffer=Buffer.from(base64,"base64");const blob=new Blob([buffer],{type:"image/png"});response=await openai.images.edit({model:"gpt-image-1.5",image:blob,prompt,size:"1024x1536",quality:"medium"})}else{response=await openai.images.generate({model:"gpt-image-1.5",prompt,size:"1024x1536",quality:"medium"})}const b64=response.data?.[0]?.b64_json;if(!b64)return NextResponse.json({error:"The image provider returned no image."},{status:502});return NextResponse.json({image:`data:image/png;base64,${b64}`,demo:false})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Generation failed."},{status:500})}}
